import { StyleSheet } from "react-native";

export const styles: { container: Object } = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    alignItems: "center",
    justifyContent: "center",
  },
  basicText: {
    fontFamily: "FiraCode_400Regular",
  },
  boldText: {
    fontFamily: "FiraCode_600SemiBold",
  },
});
